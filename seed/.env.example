# Google Maps Geocoding API Configuration
# Copy this file to .env and add your actual API key

# Get your API key from: https://console.cloud.google.com/google/maps-apis/credentials
# 1. Go to Google Cloud Console
# 2. Create a project (or select existing)
# 3. Enable "Geocoding API"
# 4. Create credentials -> API Key
# 5. Copy the key below (replace the placeholder)

GOOGLE_MAPS_API_KEY=your_api_key_here

# Optional: Path to Firebase service account JSON (if not in seed directory)
# GOOGLE_APPLICATION_CREDENTIALS=./mymeds-firebase-adminsdk.json
